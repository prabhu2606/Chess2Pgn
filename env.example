# AWS Configuration
NEXT_PUBLIC_AWS_REGION=us-east-1
NEXT_PUBLIC_AWS_S3_BUCKET=your-bucket-name

# These will be set automatically by Amplify CLI after running:
# amplify init
# amplify add storage
# amplify add function

# After running amplify push, update the bucket name above with the actual bucket name

